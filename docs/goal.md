Конечно! Давай зафиксируем всё, что мы нагенерировали, прямо здесь. Это будет наш «манифест» проекта, чтобы ничего не потерялось.

---

## 🌍 Проект «Эволюционирующий мир» (Autonomous Sandbox)

**Главная цель:**
Создание серверной «песочницы», в которой цифровые сущности живут, развиваются и эволюционируют полностью автономно. Мир должен уметь модифицировать собственный код и логику через AI-агентов (LLM) в режиме реального времени.

---

### 🚀 Основные задачи

1. **Разработка ядра (Backend):**
* Язык: **Python** (как основной для логики и интеграции с AI).
* Реализация жизненного цикла: рождение, рост, клонирование, деградация и смерть.
* Создание системы «памяти» мира, чтобы эволюция была последовательной.


2. **Интеграция с AI-агентами (LLM):**
* Использование **локальных LLM** (например, Llama 3 или Mistral) для управления логикой.
* **Self-Modifying Code:** реализация механизма, где LLM анализирует состояние мира и правит код (классы, функции) прямо в рантайме, чтобы оптимизировать «жизнь» сущностей.
* Автономность: мир работает 24/7, даже если пользователь не залогинен.


3. **Визуализация и UI:**
* Создание веб-приложения для мониторинга.
* **Визуальный стиль:** Сущности, вдохновленные образом **Molbot** (кругленькие, с ушками).
* Отображение иерархий: как мелкие существа объединяются в команды, корпорации или целые страны.


4. **Механика взаимодействия:**
* **Эволюция:** Сущности могут менять форму, функции и поведение.
* **Интерактивность:** Возможность для человека «вмешиваться» в процесс (помогать или менять условия среды), работая в связке с AI.



---

### 🧬 Как это будет работать (Концепт)

* **Мир:** Это сервер, где крутится бесконечный цикл симуляции.
* **Агент (Мозг):** Локальная LLM выступает в роли «Природы» или «Архитектора». Она получает логи событий и решает: «Так, эти сущности слишком быстро умирают, давай-ка я изменю функцию `calculate_energy` в коде».
* **Сущности:** Объекты, которые стремятся к выживанию и усложнению своей структуры.

---

> **Заметки по стеку:** > Бэкенд на Python обеспечит гибкость для работы с кодом «на лету», а фронтенд будет служить лишь «окном» в этот живой цифровой аквариум.

Хочешь, чтобы я теперь расписал подробнее архитектуру того, **как именно Python будет позволять LLM менять свой код в рантайме** (через динамический импорт или `exec/eval`), чтобы это не "сломало" весь сервер?